server:
  port: 8888

spring:
  application:
    name: study-JUC
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/study_juc?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml